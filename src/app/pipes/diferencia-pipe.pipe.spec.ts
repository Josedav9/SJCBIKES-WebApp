import { DiferenciaPipePipe } from './diferencia-pipe.pipe';

describe('DiferenciaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DiferenciaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
